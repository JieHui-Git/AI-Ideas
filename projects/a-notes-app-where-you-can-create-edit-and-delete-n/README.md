# Notes App

This is a simple notes application built using React + Vite for the frontend and Express + JSON file storage for the backend. Users can create, edit, and delete notes.

## Setup

1. Install dependencies:
   